<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <item>

        <shape >
            <solid android:color="@color/colorPrimaryLight" />
            <corners android:radius="@dimen/spacing_2"/>

        </shape>

    </item>

    <item>

        <clip>

            <shape
                >

                <gradient
                    android:angle="45"
                    android:endColor="@color/accentTwo"
                    android:centerColor="@color/accentTwo"
                    android:startColor="@color/accentFour"
                    />

                <corners android:radius="@dimen/spacing_2"/>
            </shape>

        </clip>

    </item>
</layer-list>